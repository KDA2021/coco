#include<stdio.h>/*这个是改进版，用来解决输入520变成025的问题*/
int main()
{
	int m, n;/*一个解决方法是把结果存储在变量m中，然后用%d输出或者用%3d输出*/
	scanf_s("%d", &n);
	m = (n % 10) * 100 + (n / 10 % 10) * 10 + n / 100;
	printf("%03d", m);/*%3d表示输出三个字符长度的整数*/
	return 0;
}
